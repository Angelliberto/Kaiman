# Solo en Vercel (sin prefijo VITE_). URL del backend en Koyeb.
# El proxy en middleware.ts reenvía /api y /images; el navegador nunca ve esta URL.
# Con o sin https://; sin /api al final.
API_URL=https://tu-api.koyeb.app
